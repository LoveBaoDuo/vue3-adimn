import { breadConfigType } from '../types/index'
// 面包屑的基础配置
export const breadDefaultConfig: breadConfigType = {
  separator: '',
  type: 'default',
}
export const breadTagConfig: breadConfigType = {
  separator: '',
  type: 'tag',
}
